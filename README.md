Para utilizar la API, primero deberás hacer una petición POST a la siguiente ruta:

/logs/authorizations

Obtendrá un token que deberá colocar en el header "Authorization", de modo que el resto de peticiones funcionen.